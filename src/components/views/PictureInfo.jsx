import React from 'react';
import styled from 'styled-components';

const Heading = styled.h1`
  color: black;
  font-size: 2em;
  font-weight: bold;
`;

const PictureInfo = () => {
    return <Heading>PictureInfo component</Heading>
}

export default PictureInfo;