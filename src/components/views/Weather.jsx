import React from 'react';
import styled from 'styled-components';

const Heading = styled.h1`
  color: black;
  font-size: 2em;
  font-weight: bold;
`;

const Weather = () => {
    return <Heading>Weather component</Heading>
}

export default Weather;